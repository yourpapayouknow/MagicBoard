# MagicBoard 会话历史

- 会话编号：`01a07132-ee6f-74f2-b2f4-7970106d1a3e`
- 项目目录：`/Users/mac/codexproj/magicboard`
- 开始时间：2026-09-05 18:59（Asia/Shanghai）

### 第 1 轮对话（2026-09-05 18:59）

- **Who（谁参与）**：用户 + AI（Codex Assistant）。
- **What（做了什么）**：为键盘字母区新增“字母键下滑输入大写”设置开关。修改共享设置模型及兼容解码、主 App“按键体验”页面、中英文本地化、键盘字母键手势注册与处理入口，并新增默认值、设置往返和旧配置兼容测试。
- **When（何时发生）**：2026-09-05 18:59（Asia/Shanghai）。
- **Where（在哪个上下文）**：工作目录 `/Users/mac/codexproj/magicboard`；涉及 `Packages/MagicBoardShared/Sources/MagicBoardShared/SharedConfig.swift`、`Packages/MagicBoardShared/Tests/MagicBoardSharedTests/SharedConfigTests.swift`、`App/MagicBoardApp.swift`、`App/en.lproj/Localizable.strings`、`App/zh-Hans.lproj/Localizable.strings` 与 `Keyboard/KeyboardViewController.swift`。
- **Why（目的/背景）**：用户快速输入时容易误触字母键滑动大写，需要允许主动启用或关闭该交互。
- **How（如何实现/决策过程）**：先检查 Git、日志/历史、`DESIGN.md` 和 CodeGraph 调用链；经用户确认将首次安装及旧版本升级后的默认状态设为关闭。关闭时字母键不注册滑动识别器，处理入口另加保护；符号键上层输入和 F1–F12 下滑功能保持不变。共享配置 77 项测试最终全部通过，设计 lint 为 0 项，两个本地化文件校验通过，完整模拟器 Debug 构建成功；首次完整测试中的既有内存基准一度波动至 112 KB，重跑为 80 KB 并通过 100 KB 阈值。

### 第 2 轮对话（2026-09-05 19:26）

- **Who（谁参与）**：用户 + AI（Codex Assistant）。
- **What（做了什么）**：修复横向滚动后较后位置候选无法点击提交的问题；新增 App“自定义词典”页面，支持按当前输入方案手工添加、搜索和删除词条，导入 UTF-8 Rime `.dict.yaml` 与 TSV 词典，以及清空学习记录；在键盘候选管线中接入个人词条并按持久化选择次数排序。
- **When（何时发生）**：2026-09-05 19:26（Asia/Shanghai）。
- **Where（在哪个上下文）**：工作目录 `/Users/mac/codexproj/magicboard`；主要涉及 `Keyboard/RimeEngine.swift`、`Keyboard/KeyboardViewController.swift`、`Packages/MagicBoardShared/Sources/MagicBoardShared/PersonalDictionary.swift`、`Packages/MagicBoardShared/Tests/MagicBoardSharedTests/PersonalDictionaryTests.swift`、`App/MagicBoardApp.swift` 及中英文本地化文件。
- **Why（目的/背景）**：候选条将全局候选位置传给只接受当前页下标的 LibrimeKit API，导致第一页之后的候选选择失败；用户同时需要可从主 App 管理并导入的个人常用词，以及能够根据实际选择自动调整候选顺序的记忆功能。
- **How（如何实现/决策过程）**：先从历史记录、代码结构和固定版本 LibrimeKit 源码确认根因，再经用户确认导入格式、当前方案编码与全候选来源学习范围。选择 App Group SQLite 作为轻量共享存储，使用 WAL、索引和 4096 条学习记录上限，避免把大词典整体载入键盘扩展；候选按钮保存原始 Rime 全局下标，点击时按页大小换算页码和页内下标后翻页选择。新增解析、校验、合并、方案隔离、持久化学习、搜索删除和分页换算测试。最终 86 项测试全部通过，Jetsam 基准复跑为 96 KB，设计 lint、本地化校验、差异校验以及有/无签名模拟器 Debug 构建均通过；在 iPad 模拟器中实际完成 `魔法键盘 / mofajianpan / 80` 的添加与列表显示验证。
