# MagicBoard 会话历史

- 会话编号：`01a0800b-a325-75c0-9be5-16deb981416c`
- 项目目录：`/Users/mac/codexproj/magicboard`
- 开始时间：2026-09-08 16:06（Asia/Shanghai）

### 第 1 轮对话（2026-09-08 16:19）

- **Who（谁参与）**：用户 + AI（Codex Assistant）。
- **What（做了什么）**：修复键盘扩展每次呼出都重置为英文的问题。在 `/Users/mac/codexproj/magicboard/Packages/MagicBoardShared/Sources/MagicBoardShared/SharedConfig.swift` 新增中英文状态锁读写，在 `/Users/mac/codexproj/magicboard/Keyboard/KeyboardViewController.swift` 接入初始化恢复、切换即时保存、离场保存及关闭中文输入时强制英文，在共享测试中新增默认值、中文往返和损坏值回退覆盖；版本升级为 `1.0.4 (23)`。完成提交 `d05bb8d`、推送 `master` 与注释标签 `v1.0.4`，发布 GitHub Release `https://github.com/yourpapayouknow/MagicBoard/releases/tag/v1.0.4`，资产为 `MagicBoard.tipa`、`cpmac`、`cpwin.exe`。
- **When（何时发生）**：2026-09-08 16:06–16:19（Asia/Shanghai）。
- **Where（在哪个上下文）**：项目 `/Users/mac/codexproj/magicboard`，共享模块、键盘扩展、XcodeGen 版本配置、GitHub 仓库 `yourpapayouknow/MagicBoard`。
- **Why（目的/背景）**：用户报告键盘每次呼出总是英文，希望增加状态锁，记住上次关闭时的中文或英文状态，并要求发布更新。
- **How（如何实现/决策过程）**：先确认 Git 工作区干净并检查构建日志、历史记录与 `DESIGN.md`，再用 CodeGraph 定位 `KeyboardViewController`、`InputState` 和生命周期路径。源码直接证明 `state = InputState()` 每次以 `.english` 初始化且语言切换只存在内存中。先添加预期编译失败的红测，再复用现有 App Group `UserDefaults` 以键 `magicboard.keyboard.language` 保存 `BoardLang.rawValue`；首次或损坏值回退英文，中文输入关闭时锁定英文。新增测试通过，全套 88/88 通过；`DESIGN.md` lint 为 0 findings；iOS 模拟器 Debug 与 arm64 Release 构建成功；TIPA 版本、架构、App Group、私有 HID 权限、Rime 资源和 ZIP 完整性通过，大小 17,485,678 bytes，SHA-256 为 `ce99a252bcf11edbbcd56b492e8e8c0bdb30cd1f2b703a4c54c9f23f1ba54ec5`。GitHub 远端资产大小与三个本地 SHA-256 全部一致。
